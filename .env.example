# Spotify Web API credentials for the resolve:* scripts.
# Create an app at https://developer.spotify.com/dashboard, then copy this file
# to .env and paste the real values. .env is gitignored — never commit it.
#
# Used by: pnpm resolve:spotify, pnpm resolve:artists
SPOTIFY_CLIENT_ID=
SPOTIFY_CLIENT_SECRET=
